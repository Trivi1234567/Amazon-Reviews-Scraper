# ── Minimal, guaranteed-to-work dependency list ───────────
pandas
requests               # <- hard requirement for HTTP calls
beautifulsoup4         # <- HTML parsing inside the scraper
amazon-product-review-scraper==0.9
